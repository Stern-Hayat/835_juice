// 1.5リットル入りのジュースを𝑁本買ってそのジュースを1リットル入りの空容器𝑀本に移しかえた。
// 移しかえた後の1リットル入りの𝑀本の容器はすべて満杯で、容器に入らなかった余りのジュースは1リットル以上なかった。
// 𝑁が与えられるので𝑀を求めよ。

#include <stdio.h>
int main(void){
    int M,N;
    float O,sum;
    scanf("%d",&N);
    
    sum = 1.5*N;
    M = sum / 1;
    O = sum - M;
    
    if (O > 1.0){
        printf("容器に入ったジュースが1L以上でした。");
        return 0;
    }
    
    printf("Mは%d本です。あまりは%.1f",M,O);
    return 0;
    
}
